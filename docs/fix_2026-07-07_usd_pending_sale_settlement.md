# USD 미결제 매도대금(T+2) 누락 — 대량 매도일 가짜 드로우다운 수정

> **요약**: ① 개별주 접기로 07-07 새벽 USD 개별주(NVDA/TSLA/PLTR/LLY ≈$19,229)를 매도한 뒤, 전체 자산이 222.3M→193.7M로 떨어지고 드로우다운이 −13.76%로 찍혀 실제 손실처럼 보였다. ② 원인은 잔고 조회의 **USD 예수금 계산이 `withdrawable_amount`(frcr_drwg_psbl_amt_1)만 써서 당일 매도대금(frcr_sll_amt_smtl, T+2 결제 전)을 통째로 누락**한 것 — KRW쪽은 이미 `prvs_rcdl_excc_amt`로 미결제 매도를 반영하는데 USD만 빠져 있었다(실제 총자산은 130 ISA+14 KRW_1+78 USD=222M로 무손실, 미결제 $19,229=2,950만원이 안 세어졌을 뿐). ③ USD 예수금에 `frcr_sll_amt_smtl`을 더하도록 수정(KRW `prvs_rcdl_excc_amt`와 동일 취지). 실조회 검증: 전체 193.7M→**222.7M**, USD 49M→**78.4M**, 드로우다운 −13.76%→**−0.89%**로 정정.

## 증상

- 2026-07-07 03:01 개별주 매도 후, 10:00 KRW 런에서 `total_all_krw` 193.7M, 드로우다운 **−13.76%**.
- 3시간 전(매도 직전)엔 −1.05%였고 시장 폭락도 없었음(레짐 Goldilocks).

## 진단 (`scripts/diag_kis_balance_fields.py` 실조회)

USD 계좌(64378890-01, country=US) 필드:
- `frcr_dncl_amt_2`(예수금) = `frcr_drwg_psbl_amt_1`(출금가능) = **$1,019**  ← pykis deposit이 노출, 시스템이 씀
- `frcr_sll_amt_smtl`(매도금액합계, 미결제) = **$19,229**  ← 개별주+잔챙이 매도대금, T+2 결제 전
- 보유종목 평가 = $31,102(47.6M) / 실제 USD 총액 = 47.6+1.56(예수금)+29.5(미결제) = **78.6M**

계좌 재구성: KRW_1 14.68M + ISA(KRW_2) 129.6M + USD 78.6M = **222.9M**(무손실). 시스템은 USD를
49.2M(미결제 누락)로 봐서 193.5M로 저평가 → 가짜 −13.76% 드로우다운.

## 수정 (`trading/executor.py` get_portfolio_state)

USD 예수금 계산에 `frcr_sll_amt_smtl`(미결제 매도대금)을 더한다 — KRW `prvs_rcdl_excc_amt`와 동일 취지.
매수증거금은 `withdrawable_amount`에서 이미 차감돼 있어 중복 아님. raw output2에서 추출, 실패 시 폴백(기존 동작).

```
cash_usd = withdrawable_amount (+ frcr_sll_amt_smtl if >0)
```

## 검증 (실조회)

| | 수정 전 | 수정 후 |
|---|---|---|
| 전체 자산 | 193.7M | **222.7M** |
| USD 계좌 | 49M | **78.4M** |
| 드로우다운 | −13.76% | **−0.89%** |

`[USD 미결제 매도대금] +$19,229 반영 (T+2 결제 대기)` 로그로 반영 확인.

## 영향·주의

- 드로우다운·총자산·USD비중이 대량 매도일에도 정확 → 오늘 같은 가짜 드로우다운, USD 저평가로 인한
  불필요한 역합성/오버플로 churn 해소.
- **모니터링 필요**: `frcr_sll_amt_smtl`이 결제 완료(T+2)까지 유지되는지 vs 익일 리셋인지 미확인.
  익일 리셋이면 결제 중간일(화)에 갭이 재발할 수 있음 → 07-08~09 총자산이 222M 유지되는지 관찰,
  갭 재발 시 자체 매도추적(T+2 감쇠) 등 보강. drawdown_scaling OFF라 가짜 드로우다운이 강제매도를
  유발하진 않음(안전).
