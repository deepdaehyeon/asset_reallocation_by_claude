# ────────────────────────────────────────────────
# 자동 자산 배분 시스템 설정
# ────────────────────────────────────────────────

# 레짐 감지용 시장 데이터 (yfinance)
signal:
  tickers: ["SPY", "^VIX", "TLT", "HYG", "DX-Y.NYB", "DJP"]
  lookback_days: 130          # 모멘텀 6M 계산에 필요한 최소 거래일
  # DX-Y.NYB: 달러 인덱스(DXY) / DJP: Bloomberg Commodity Index ETN

# KIS 계좌 정의
accounts:
  KRW_1:
    acc_no: "64378890-01"
    currency: KRW
  KRW_2:
    acc_no: "64521213-01"
    currency: KRW
  USD:
    acc_no: "64378890-01"   # KRW_1과 동일 계좌 — USD 포지션 전용
    currency: USD

# 투자 유니버스
universe:
  # ── Equity ETF (KRW) ─────────────────────────────────────────────────────
  "379800":
    name: "KODEX S&P500"
    currency: KRW
    asset_class: equity_etf
  "379810":
    name: "KODEX 나스닥100"
    currency: KRW
    asset_class: equity_etf
  # ── Equity Factor (USD) — Value / 소형가치 팩터 ──────────────────────────
  "VTV":
    name: "Vanguard Value ETF"
    currency: USD
    exec_account: USD
    asset_class: equity_factor
  "AVUV":
    name: "Avantis U.S. Small Cap Value ETF"
    currency: USD
    exec_account: USD
    asset_class: equity_factor
  # ── Equity Sector (USD) — 에너지 섹터 (인플레 헤지) ──────────────────────
  "XLE":
    name: "Energy Select Sector SPDR ETF"
    currency: USD
    exec_account: USD
    asset_class: equity_sector
  # ── Equity Individual (USD) — 4종목 다각화 (EV/AI/반도체/헬스케어) ────────
  "TSLA":
    name: "Tesla"
    currency: USD
    exec_account: USD
    asset_class: equity_individual
  "PLTR":
    name: "Palantir"
    currency: USD
    exec_account: USD
    asset_class: equity_individual
  "NVDA":
    name: "Nvidia"
    currency: USD
    exec_account: USD
    asset_class: equity_individual
  "LLY":
    name: "Eli Lilly"
    currency: USD
    exec_account: USD
    asset_class: equity_individual
  # ── Equity Developed ex-US (USD) — 글로벌 분산 ────────────────────────────
  "VEA":
    name: "Vanguard FTSE Developed Markets ex-US"
    currency: USD
    exec_account: USD
    asset_class: equity_developed
  # ── Equity Emerging Markets (USD) — 신흥국 분산 ──────────────────────────
  "VWO":
    name: "Vanguard FTSE Emerging Markets"
    currency: USD
    exec_account: USD
    asset_class: equity_emerging
  # ── Bond (USD) ───────────────────────────────────────────────────────────
  "IEF":
    name: "iShares 7-10Y Treasury"
    currency: USD
    exec_account: USD
    asset_class: bond_usd
  "SHY":
    name: "iShares 1-3Y Treasury"
    currency: USD
    exec_account: USD
    asset_class: bond_usd
  # ── Bond TIPS (USD) — 인플레 연동채 (Stagflation 헷지) ────────────────────
  "VTIP":
    name: "Vanguard Short-Term TIPS"
    currency: USD
    exec_account: USD
    asset_class: bond_tips
  # ── Bond (KRW) ──────────────────────────────────────────────────────────
  "305080":
    name: "TIGER 미국채10년"
    currency: KRW
    asset_class: bond_krw
  # ── Gold (KRW) ──────────────────────────────────────────────────────────
  "411060":
    name: "ACE KRX금현물"
    currency: KRW
    asset_class: gold
  # ── Commodities (USD) ────────────────────────────────────────────────────
  "DBC":
    name: "Invesco DB Commodity Index"
    currency: USD
    exec_account: USD
    asset_class: commodity
  # ── Managed Futures (USD) ────────────────────────────────────────────────
  "DBMF":
    name: "iMGP DBi Managed Futures Strategy"
    currency: USD
    exec_account: USD
    asset_class: managed_futures
  # ── Cash (KRW) ──────────────────────────────────────────────────────────
  "469830":
    name: "SOL 초단기채권"
    currency: KRW
    asset_class: cash
  # ── Synthetic Bridge (KRW) — T+2 합성 노출 전용, regime_targets 미포함 ────
  "261220":
    name: "KODEX WTI원유선물(H)"
    currency: KRW
    asset_class: commodity_krw

# ────────────────────────────────────────────────────────────────────────────
# 동적 자산 배분 — 레짐별 자산군 목표 비중 (합계 = 1.0)
#
# ※ 실제 실행 시에는 HMM 사후 확률로 가중 평균(Continuous Exposure)이 적용된다.
#    이 값은 극단 레짐(확률 100%)일 때의 순수 배분이다.
#
# 포트폴리오 구조:
#   Core 80%     — equity_etf / equity_factor / bond / gold / commodity / managed_futures / cash
#   Satellite 20% — equity_individual (TSLA + PLTR: 장기투자 + 알파)
#
# 자산군 설명:
#   equity_etf        : KODEX S&P500 + 나스닥100 (KRW, 시장 베타)
#   equity_factor     : VTV(Value ETF) (USD, Reflation·금리 상승 수혜)
#   equity_individual : TSLA + PLTR (USD, 고베타 성장주 = Satellite)
#   commodity         : DBC (USD, 인플레 헤지 — Reflation·Stagflation 핵심)
#   managed_futures   : DBMF (USD, CTA — Slowdown·Stagflation·Crisis 방어)
#   bond_usd          : IEF + SHY (USD 중단기채 — Slowdown·Crisis 방어)
#   bond_krw          : TIGER 미국채10년 (KRW — Slowdown·Crisis panic bid)
#   gold              : ACE KRX금현물 (KRW, 전천후 헤지)
#   cash              : SOL 초단기채권 (KRW, Crisis·Stagflation 방어)
# ────────────────────────────────────────────────────────────────────────────

regime_targets:
  # ── Goldilocks: 성장↑ + 유동성↑ (인플레 안정) ──────────────────────────────
  # 가장 Risk-On. cash 축소(15→8), equity_etf 확장(40→42), 해외 분산 추가.
  Goldilocks:          # core 80% | satellite 20%
    equity_etf:        0.42   # S&P500 + 나스닥100 — 광범위 성장 (cash에서 흡수)
    equity_factor:     0.05   # VTV+AVUV 소량
    equity_sector:     0.00
    equity_individual: 0.20   # TSLA+PLTR+NVDA+LLY (4종목 분산)
    equity_developed:  0.03   # VEA — 유럽·일본 분산
    equity_emerging:   0.02   # VWO — 신흥국 분산
    commodity:         0.05
    managed_futures:   0.05
    bond_usd:          0.00
    bond_tips:         0.00   # 인플레 안정 → TIPS 불필요
    bond_krw:          0.00
    gold:              0.10
    cash:              0.08   # 0.15 → 0.08 (Goldilocks 현금 과다 축소)

  # ── Reflation: 성장↑ + 인플레↑ ─────────────────────────────────────────────
  # TIPS 추가 (인플레 헷지). 해외 분산 소량.
  Reflation:           # core 90% | satellite 10%
    equity_etf:        0.22
    equity_factor:     0.08
    equity_sector:     0.07
    equity_individual: 0.10
    equity_developed:  0.02
    equity_emerging:   0.02
    commodity:         0.16
    managed_futures:   0.05
    bond_usd:          0.00
    bond_tips:         0.05   # VTIP — 인플레 상승 헷지
    bond_krw:          0.00
    gold:              0.15
    cash:              0.08   # 0.17 → 0.08

  # ── Slowdown: 성장↓ (인플레 낮음) ──────────────────────────────────────────
  # 채권·금·DBMF 방어. 해외 분산 소량. TIPS 불필요(저인플레).
  Slowdown:            # core 90% | satellite 10%
    equity_etf:        0.15
    equity_factor:     0.05
    equity_sector:     0.00
    equity_individual: 0.10
    equity_developed:  0.02
    equity_emerging:   0.02
    commodity:         0.05
    managed_futures:   0.12
    bond_usd:          0.12
    bond_tips:         0.00
    bond_krw:          0.15
    gold:              0.14
    cash:              0.08   # 0.12 → 0.08

  # ── Stagflation: 성장↓ + 인플레↑ ───────────────────────────────────────────
  # TIPS 대량 추가 (핵심 인플레 방어). cash 축소.
  Stagflation:         # core 92% | satellite 8%
    equity_etf:        0.05
    equity_factor:     0.03
    equity_sector:     0.05
    equity_individual: 0.08
    equity_developed:  0.02
    equity_emerging:   0.02
    commodity:         0.18
    managed_futures:   0.12
    bond_usd:          0.05
    bond_tips:         0.08   # VTIP — Stagflation 핵심 자산
    bond_krw:          0.00
    gold:              0.18
    cash:              0.14   # 0.26 → 0.14

  # ── Crisis: 유동성 쇼크 ─────────────────────────────────────────────────────
  # TIPS 회피 (유동성 위기엔 동반 매도). 해외 분산 극소.
  Crisis:              # core 95% | satellite 5%
    equity_etf:        0.00
    equity_factor:     0.03
    equity_sector:     0.00
    equity_individual: 0.05
    equity_developed:  0.02
    equity_emerging:   0.02
    commodity:         0.05
    managed_futures:   0.12
    bond_usd:          0.10
    bond_tips:         0.00
    bond_krw:          0.10
    gold:              0.15
    cash:              0.36   # 0.40 → 0.36 (소폭 축소)

# 자산군 → 종목 매핑 (within-class 상대 비중, 고정)
asset_routing:
  equity_etf:          # KRW 계좌 전용
    "379800": 0.64
    "379810": 0.36
  equity_factor:       # USD 계좌 전용 — VTV(Large Value) + AVUV(Small Value)
    VTV:  0.60
    AVUV: 0.40
  equity_sector:       # USD 계좌 전용 — XLE(에너지) 100%
    XLE:  1.00
  equity_individual:   # USD 계좌 전용 — 4종목 분산 (EV/AI/반도체/헬스케어)
    NVDA: 0.30
    LLY:  0.25
    PLTR: 0.25
    TSLA: 0.20
  equity_developed:    # USD 계좌 전용 — 선진 ex-US
    VEA:  1.00
  equity_emerging:     # USD 계좌 전용 — 신흥국
    VWO:  1.00
  commodity:           # USD 계좌 전용
    DBC:  1.00
  managed_futures:     # USD 계좌 전용
    DBMF: 1.00
  bond_usd:            # USD 계좌 전용
    IEF:  0.58
    SHY:  0.42
  bond_tips:           # USD 계좌 전용 — 인플레 연동채
    VTIP: 1.00
  bond_krw:            # KRW 계좌 전용
    "305080": 1.00
  gold:                # KRW 계좌 전용
    "411060": 1.00
  cash:                # KRW 계좌 전용
    "469830": 1.00

# dry-run 또는 계좌 조회 실패 시 사용할 KRW/USD 비율 폴백
account_ratio_fallback:
  usd: 0.30
  krw: 0.70

# ── 자산군별 최대 비중 상한 ──────────────────────────────────────────────────
# 레짐 블렌딩으로 인한 과집중 방지. 초과분은 cash로 이동.
class_max_weight:
  equity_individual: 0.20   # Satellite 20% 상한 (4종목 분산으로 종목당 5~6%)
  equity_developed:  0.05   # VEA — 글로벌 분산, 작은 비중 유지
  equity_emerging:   0.03   # VWO — 신흥국, 더 작은 비중
  commodity:         0.20   # DBC — Stagflation 20%가 최대
  managed_futures:   0.12   # DBMF — 횡보장 약점 고려
  bond_tips:         0.10   # VTIP — Stagflation 8%가 최대, 약간의 여유
  gold:              0.18   # 전천후 헤지 — 과대 비중 방지

# ── 변동성 타겟팅 ────────────────────────────────────────────────────────────
# portfolio EWMA vol 기반 (SPY realized vol 아님)
# regime_target_vol: 레짐별 목표 변동성 (레짐 미지정 시 target_vol 사용)
# floor: 최소 equity 유지율 (1.0 - floor = 최대 축소율)
vol_targeting:
  enabled: true
  target_vol:  0.10        # 레짐 미지정 시 기본값
  regime_target_vol:
    Goldilocks:  0.13      # 성장 국면: 리스크 허용도 높음
    Reflation:   0.11
    Slowdown:    0.09
    Stagflation: 0.08
    Crisis:      0.06      # 위기 국면: 리스크 최소화
  use_portfolio_vol: true  # true: EWMA 포트폴리오 변동성 / false: SPY realized vol
  ewma_lambda: 0.94        # RiskMetrics 표준 λ
  floor:       0.65        # equity 최대 35% 축소
  equity_asset_classes:
    - equity_etf
    - equity_factor
    - equity_sector
    - equity_individual
    - equity_developed
    - equity_emerging

# HMM + RF 앙상블 레짐 모델
hmm:
  enabled: true
  lookback_days: 500       # HMM 학습 윈도우 (거래일)
  predict_lookback: 60     # 추론 시 사용할 시퀀스 길이 — 전이 확률 문맥 반영
  min_samples: 100
  override_threshold: 0.60
  # Crisis 비대칭 우선 (transition 후행 손실 완화 — 진단 결과 후속 작업):
  #   null/누락 — 비대칭 비활성 (기존 동작, 일반 override만)
  #   값 (예: 0.30) — blend["Crisis"]가 이 임계 이상이면 다른 조건 무시하고 Crisis 채택
  crisis_priority_threshold: null
  # HMM 1-step ahead 예측 사용 (transition matrix로 next-step 분포):
  #   false — 마지막 시점 사후 확률 (기존)
  #   true  — transition 적용한 next-step 분포 (forward 시그널)
  use_forward_hmm: false
  forward_hmm_horizon: 1
  rf_enabled: true         # BalancedRF 앙상블 (소수 레짐 탐지 보강)
  rf_weight: 0.40          # RF 확률 가중치 (HMM 0.6 + RF 0.4)
  # RF 라벨링 방식:
  #   rf_forward_window=0 (기본): 동일 시점의 detect_regime() 라벨 — 룰의 근사기 (자기참조)
  #   rf_forward_window=N>0 + rf_label_mode='rule_at_future':
  #     t+N 시점의 detect_regime() 라벨 — forward-looking, 자기참조 끊김(룰 임계 그대로)
  #   rf_forward_window=N>0 + rf_label_mode='quantile':
  #     t+N 시점의 (momentum_1m, realized_vol) 학습 분포 분위 매핑 — detect_regime 호출 없음
  # 활성화 전 scripts/compare_rf_label.py로 백테스트 비교 권장.
  rf_forward_window: 0
  rf_label_mode: rule_at_future   # rule_at_future | quantile
  # state→regime 매핑 방식:
  #   true (기본): HMM state별 피처 통계로 비지도 매핑 (detect_regime 자기참조 제거)
  #                매핑이 ambiguous하면 legacy로 자동 폴백
  #   false      : legacy detect_regime 다수결 매핑만 사용
  unsupervised_mapping: true
  # Crisis state 식별 임계 — 룰 기반 detect_regime의 rvol>0.30과 통일.
  # state 평균 realized_vol이 threshold 이상이거나, 2위 state 대비 ratio배 이상 높을 때만 Crisis.
  crisis_rvol_threshold: 0.30
  crisis_rvol_ratio: 1.5
  # _unsupervised_state_mapping에서 growth_score / infl_score 합성에 쓰는 가중치.
  # 가중치를 외부화한 이유: mom1m(±0.05 스케일) 대비 curve(±2 스케일)·vix(±20 스케일)가
  # 그대로면 한쪽이 점수를 지배 → 캘리브레이션 가능하도록 노출.
  mapping_weights:
    growth_curve:     0.01   # (curve_10y2y - 0.5) * w  → 가파른 커브 가중
    infl_commodity:   1.0    # commodity_mom_1m 가중 (그대로)
    infl_hy_zscore:   0.03   # hy_spread_zscore * w
    infl_cpi:         0.01   # (cpi_yoy - 2.0) * w
    infl_vix:         0.005  # (vix - 20.0) * w   (기존 1/200 = 0.005)

# 이상 탐지 (Option C — detect_regime 자기참조 없는 독립 신호)
# IsolationForest로 현재 시장이 학습 분포에서 얼마나 벗어났는지 측정.
# 높은 anomaly_score → 레짐 신뢰도 하향 → confidence_threshold(0.40) 미달 시 이전 확정 레짐 유지.
anomaly:
  enabled: true
  contamination: 0.05          # IsolationForest 가정 이상치 비율 (학습 시)
  confidence_penalty: 0.5      # anomaly=1.0일 때 신뢰도를 50% 깎음 (선형 감쇠)

# 레짐 전환 히스테리시스 필터 (표시·알림 목적 — 실제 비중은 연속 블렌딩 적용)
# 신뢰도 미달 시 폴백 정책: 이전 확정 레짐(state.confirmed_regime)을 유지하고,
# 초기 실행 등 이전 레짐이 없을 때만 DEFAULT_REGIME(Slowdown)로 폴백.
# 이유: 항상 Slowdown 폴백은 강세장 초입에서 신뢰도가 낮을 때 기회를 놓치는 체계적 편향.
regime_filter:
  confirmation_count: 3
  cooldown_days: 5
  # confidence_threshold: confidence_method 스케일에 맞춰야 한다.
  #   mean  : 0.40 (기존). 비단조 산식이므로 실제 캘리브레이션 의미 약함.
  #   min   : 0.20 (현재). mean 0.40과 동등한 fallback 비율(~39%) 유지.
  #   product: 0.10 권장 (multiplicative라 더 낮은 임계 필요).
  confidence_threshold: 0.20
  # 신뢰도 결합 산식 (외부 비평 #4):
  #   mean    — (rule_conf + hmm_conf) / 2  (Spearman ρ -0.325, 비단조)
  #   min     — min(rule_conf, hmm_conf)    (ρ +0.595, 단조 회복) ← 현재
  #   product — rule_conf * hmm_conf        (ρ +0.681, 가장 강한 단조)
  confidence_method: min
  # blend_probs EWMA 평활 (외부 비평 #6-c whipsaw 억제):
  #   new_blend = α · prev_blend + (1-α) · raw_blend
  #   0    — 평활 없음
  #   0.5  — 약한 평활 ← 현재 (whipsaw 57.6→47.3%, Sharpe -0.013, MaxDD -1.18pp)
  #   0.7~0.9 — 강한 평활 (MaxDD 악화 폭이 커짐 — 보류)
  blend_smoothing_alpha: 0.5
  # 레짐별 override — Crisis는 빠른 진입이 중요하므로 confirmation 1회·쿨다운 0.
  per_regime:
    Crisis:
      confirmation_count: 1
      cooldown_days: 0

# 리밸런싱 파라미터
rebalancing:
  drift_threshold:            0.05   # 계좌 내 drift > 5%p 시 리밸런싱 트리거
  per_ticker_drift_threshold: 0.05   # 개별 종목 목표 대비 ±5% 이탈 시만 주문 생성
  min_order_krw: 10000
  usd_krw_fallback: 1380.0
  min_rebalance_interval_days: 7     # 마지막 리밸런싱 후 최소 7일 쿨다운 (드로우다운 비상·deferred_buys 시 무시)
  max_run_turnover: 0                # 단일 실행 회전율 상한 (0=무제한) — 6월부터 0.30으로 복구 예정
  max_monthly_turnover: 0            # 월간 누적 회전율 상한 (0=무제한, 매월 1일 자동 초기화) — 6월부터 0.30으로 복구 예정
  usd_cash_min: 0.01                 # USD 계좌 최소 현금 비율 (1%) — 수수료·환전 여유분
  krw_cash_min: 0.01                 # KRW 계좌 최소 현금 비율 (1%) — 수수료·거래 거절 방지

# 리스크 제어
# severe: equity를 floor(10%)까지 축소, 채권·금·현금 유지 — 반등 구간 미스 방지
# moderate: equity 40% 수준으로 축소 (Risk-Off 강제 효과)
# mild: equity 75% 유지
risk:
  drawdown_thresholds:
    mild:             -0.10
    moderate:         -0.20
    severe:           -0.30
    equity_floor_pct:  0.10  # severe 시 equity 최소 10% 유지 (반등 miss 방지)
  equity_asset_classes:
    - equity_etf
    - equity_factor
    - equity_sector
    - equity_individual
    - equity_developed
    - equity_emerging

# T+2 결제 지연 대응
settlement:
  buffer_tickers: ["469830"]
  buffer_min: 0.07
  synthetic_pairs:
    "TSLA": "379810"   # 나스닥100 (TSLA = NASDAQ100 구성종목)
    "PLTR": "379810"   # 나스닥100 (PLTR = NASDAQ100 구성종목)
    "NVDA": "379810"   # 나스닥100 (NVDA = NASDAQ100 핵심 종목)
    "LLY":  "379800"   # S&P500 (LLY = SPX500, 헬스케어)
    "VTV":  "379800"   # S&P500 (Value subset)
    "AVUV": "379800"   # S&P500 (소형가치 대체재 없음)
    "VEA":  "379800"   # S&P500 (선진 ex-US KRW 대체재 없음, US fallback)
    "VWO":  "379800"   # S&P500 (신흥국 KRW 대체재 없음, US fallback)
    "XLE":                 # 에너지 stocks → 금+S&P500 50/50 (인플레 헷지 + 시장 베타)
      "411060": 0.50
      "379800": 0.50
    "IEF":  "305080"   # TIGER 미국채10년 (듀레이션 일치)
    "SHY":  "469830"   # SOL 초단기채권 (단기채)
    "VTIP": "411060"   # 금 (인플레 연동채 → 인플레 헷지 자산)
    "DBC":                 # broad commodity → 금+WTI 50/50
      "411060": 0.50
      "261220": 0.50
    "DBMF": "411060"   # 금 (CTA 트렌드 헷지 → 전천후 자산)
