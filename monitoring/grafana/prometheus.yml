global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: "asset_allocation"
    static_configs:
      - targets: ["host.docker.internal:8080"]
    metrics_path: /metrics
